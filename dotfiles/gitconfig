[user]
	name = David Moody
	email = david@davidxmoody.com
[core]
	editor = nvim
	pager = diff-so-fancy | less --tabs=1,5 -R
[merge]
	tool = nvim -d
[push]
	default = simple
[credential]
	helper = cache --timeout=3600
[alias]
  lg = log --color --graph --pretty=format:'%Cred%h%Creset - %C(bold blue)%<(18)%aN %C(yellow)%d%Creset %s %Cgreen(%ar)%Creset' --abbrev-commit
[init]
	templatedir = ~/.git_templates
[include]
  path = ~/.git_include
