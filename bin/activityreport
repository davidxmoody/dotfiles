#!/bin/bash

cd ~/.dbox/Dropbox/diary-data/tracking/
for file in activity-*.txt; do
  date=${file:9:10}
  num_lines=$(wc -l < "$file")
  hours=$((num_lines/60))
  minutes=$((num_lines%60))
  printf '%s: %02dh %02dm\n' "$date" "$hours" "$minutes"
done
