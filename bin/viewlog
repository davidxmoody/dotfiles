#!/usr/bin/env bash

set -e

log_dir="$HOME/.mklog-logs"
mkdir -p "$log_dir"

chosen_log="$(ls -r "$log_dir" | fzf --select-1 --query "$*")"

if [[ -n "$chosen_log" ]]; then
  less "$log_dir/$chosen_log"
fi
